module.exports = [
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/path [external] (path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("path", () => require("path"));

module.exports = mod;
}),
"[externals]/fs [external] (fs, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("fs", () => require("fs"));

module.exports = mod;
}),
"[externals]/os [external] (os, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("os", () => require("os"));

module.exports = mod;
}),
"[project]/convex/_generated/api.js [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "api",
    ()=>api,
    "components",
    ()=>components,
    "internal",
    ()=>internal
]);
/* eslint-disable */ /**
 * Generated `api` utility.
 *
 * THIS CODE IS AUTOMATICALLY GENERATED.
 *
 * To regenerate, run `npx convex dev`.
 * @module
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$server$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/convex/dist/esm/server/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$server$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/convex/dist/esm/server/api.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$server$2f$components$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/convex/dist/esm/server/components/index.js [app-route] (ecmascript) <locals>");
;
const api = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$server$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["anyApi"];
const internal = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$server$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["anyApi"];
const components = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$server$2f$components$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["componentsGeneric"])();
}),
"[externals]/module [external] (module, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("module", () => require("module"));

module.exports = mod;
}),
"[project]/lib/convex-server.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "convexClient",
    ()=>convexClient
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$browser$2f$index$2d$node$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/convex/dist/esm/browser/index-node.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$browser$2f$http_client$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/convex/dist/esm/browser/http_client.js [app-route] (ecmascript)");
;
const convexUrl = ("TURBOPACK compile-time value", "https://ardent-nightingale-570.convex.cloud");
if ("TURBOPACK compile-time falsy", 0) //TURBOPACK unreachable
;
const convexClient = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$browser$2f$http_client$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ConvexHttpClient"](convexUrl);
}),
"[externals]/buffer [external] (buffer, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("buffer", () => require("buffer"));

module.exports = mod;
}),
"[externals]/stream [external] (stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("stream", () => require("stream"));

module.exports = mod;
}),
"[externals]/util [external] (util, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("util", () => require("util"));

module.exports = mod;
}),
"[externals]/crypto [external] (crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("crypto", () => require("crypto"));

module.exports = mod;
}),
"[project]/lib/auth.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "comparePassword",
    ()=>comparePassword,
    "generateToken",
    ()=>generateToken,
    "hashPassword",
    ()=>hashPassword,
    "verifyToken",
    ()=>verifyToken
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$jsonwebtoken$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/jsonwebtoken/index.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$bcryptjs$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/bcryptjs/index.js [app-route] (ecmascript)");
;
;
const JWT_SECRET = process.env.JWT_SECRET || "gridmind-default-secret-key-change-in-production";
console.log(`[Auth.ts] JWT_SECRET loaded: ${("TURBOPACK compile-time truthy", 1) ? JWT_SECRET.substring(0, 30) + "..." : "TURBOPACK unreachable"}`);
if (!process.env.JWT_SECRET) {
    console.warn("⚠️ JWT_SECRET not set in environment variables. Using default key. This is not secure for production!");
}
function hashPassword(password) {
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$bcryptjs$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].hashSync(password, 10);
}
function comparePassword(password, hashedPassword) {
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$bcryptjs$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].compareSync(password, hashedPassword);
}
function generateToken(userId, email, role) {
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$jsonwebtoken$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].sign({
        userId,
        email,
        role
    }, JWT_SECRET, {
        expiresIn: "7d"
    });
}
function verifyToken(token) {
    try {
        const result = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$jsonwebtoken$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].verify(token, JWT_SECRET);
        console.log(`[VerifyToken] Successfully verified token`);
        return result;
    } catch (error) {
        console.log(`[VerifyToken] Error verifying token:`, error instanceof Error ? error.message : String(error));
        return null;
    }
}
}),
"[project]/lib/server-auth.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getAuthenticatedUser",
    ()=>getAuthenticatedUser,
    "requireAuthenticatedUser",
    ()=>requireAuthenticatedUser
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$convex$2d$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/convex-server.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/auth.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/convex/_generated/api.js [app-route] (ecmascript)");
;
;
;
async function getAuthenticatedUser(request) {
    const token = request.cookies.get("auth_token")?.value ?? request.headers.get("Authorization")?.replace("Bearer ", "");
    if (!token) {
        return null;
    }
    const decoded = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["verifyToken"])(token);
    if (!decoded) {
        return null;
    }
    const user = await __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$convex$2d$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convexClient"].query(__TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["api"].users.getUserById, {
        userId: decoded.userId
    });
    if (!user) {
        return null;
    }
    return {
        id: user._id,
        email: user.email,
        name: user.name,
        role: user.role
    };
}
async function requireAuthenticatedUser(request) {
    return getAuthenticatedUser(request);
}
}),
"[project]/lib/billing-server.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "chargeCreditsForAction",
    ()=>chargeCreditsForAction,
    "refundCredits",
    ()=>refundCredits
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/convex/_generated/api.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$convex$2d$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/convex-server.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$server$2d$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/server-auth.ts [app-route] (ecmascript)");
;
;
;
async function chargeCreditsForAction(request, actionKey, note, quantity) {
    const user = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$server$2d$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["requireAuthenticatedUser"])(request);
    if (!user) {
        throw new Error("Authentication required");
    }
    const charge = await __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$convex$2d$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convexClient"].mutation(__TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["api"].billing.consumeCredits, {
        userId: user.id,
        actionKey,
        quantity,
        note
    });
    return {
        user,
        charge
    };
}
async function refundCredits(userId, transactionId, note) {
    return await __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$convex$2d$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convexClient"].mutation(__TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["api"].billing.refundCreditTransaction, {
        userId,
        transactionId: transactionId,
        note
    });
}
}),
"[project]/app/api/ai/run-column/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "POST",
    ()=>POST
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$ai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/ai/dist/index.mjs [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$ai$2d$sdk$2f$openai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@ai-sdk/openai/dist/index.mjs [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$billing$2d$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/billing-server.ts [app-route] (ecmascript)");
;
;
;
function getBillableRowCount(numRows, selectedRows) {
    if (selectedRows && selectedRows.length > 0) {
        return selectedRows.filter((row)=>row > 0 && row < numRows).length;
    }
    return Math.max(0, numRows - 1);
}
// Ã¢â€â‚¬Ã¢â€â‚¬ Helpers Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬
function getColLabel(index) {
    let label = "";
    let num = index;
    while(num >= 0){
        label = String.fromCharCode(65 + num % 26) + label;
        num = Math.floor(num / 26) - 1;
    }
    return label;
}
function extractTextFromHTML(html) {
    const sections = [];
    const jsonLdRe = /<script[^>]*type=["']application\/ld\+json["'][^>]*>([\s\S]*?)<\/script>/gi;
    let match;
    while((match = jsonLdRe.exec(html)) !== null){
        try {
            const raw = JSON.parse(match[1].trim());
            const items = Array.isArray(raw) ? raw : [
                raw
            ];
            for (const item of items){
                const lines = [];
                const val = (input)=>input ? String(input) : "";
                if (item.name) lines.push(`Name: ${val(item.name)}`);
                if (item.description) lines.push(`Description: ${val(item.description).slice(0, 240)}`);
                if (item.telephone) lines.push(`Phone: ${val(item.telephone)}`);
                if (item.email) lines.push(`Email: ${val(item.email)}`);
                if (item.url) lines.push(`Website: ${val(item.url)}`);
                if (item.address) {
                    const address = item.address;
                    if (typeof address === "string") lines.push(`Address: ${address}`);
                    else lines.push(`Address: ${[
                        address.streetAddress,
                        address.addressLocality,
                        address.addressRegion,
                        address.postalCode,
                        address.addressCountry
                    ].filter(Boolean).join(", ")}`);
                }
                if (item.openingHours) lines.push(`Opening Hours: ${Array.isArray(item.openingHours) ? item.openingHours.join(", ") : item.openingHours}`);
                if (lines.length > 0) sections.push(lines.join("\n"));
            }
        } catch  {
        // Ignore invalid JSON-LD.
        }
    }
    const metaDescription = html.match(/<meta[^>]+(?:name|property)=["'](?:description|og:description)["'][^>]+content=["']([^"']+)["']/i);
    if (metaDescription?.[1]) sections.push(`Meta Description: ${metaDescription[1].slice(0, 240)}`);
    const bodyText = html.replace(/<script[^>]*>[\s\S]*?<\/script>/gi, "").replace(/<style[^>]*>[\s\S]*?<\/style>/gi, "").replace(/<[^>]+>/g, " ").replace(/&nbsp;/g, " ").replace(/&amp;/g, "&").replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&quot;/g, '"').replace(/\s+/g, " ").trim();
    sections.push(bodyText);
    return sections.join("\n\n").trim();
}
function normalizeUrl(raw) {
    const value = raw.trim();
    if (!value) return null;
    if (/^https?:\/\//i.test(value)) return value;
    if (/^[a-z0-9.-]+\.[a-z]{2,}(\/.*)?$/i.test(value)) return `https://${value}`;
    return null;
}
// Direct search Ã¢â‚¬â€ no AI involved, guaranteed to actually hit the web
async function doSearch(query) {
    try {
        const results = [];
        const response = await fetch(`https://html.duckduckgo.com/html/?q=${encodeURIComponent(query)}`, {
            headers: {
                "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
                "Accept": "text/html,application/xhtml+xml,*/*;q=0.8",
                "Referer": "https://duckduckgo.com/"
            },
            signal: AbortSignal.timeout(10000)
        });
        if (!response.ok) return results;
        const html = await response.text();
        const titleRegex = /<a[^>]*class="result__a"[^>]*href="([^"]*)"[^>]*>([\s\S]*?)<\/a>/gi;
        const snippetRegex = /<a[^>]*class="result__snippet"[^>]*>([\s\S]*?)<\/a>/gi;
        const titleMatches = [];
        const snippets = [];
        let m;
        while((m = titleRegex.exec(html)) !== null && titleMatches.length < 5){
            let actualUrl = m[1];
            const title = m[2].replace(/<[^>]+>/g, "").trim();
            if (actualUrl.includes("uddg=")) {
                try {
                    const uddg = new URLSearchParams(actualUrl.includes("?") ? actualUrl.split("?")[1] : actualUrl).get("uddg");
                    if (uddg) actualUrl = decodeURIComponent(uddg);
                } catch  {}
            }
            if (actualUrl.startsWith("http") && !actualUrl.includes("duckduckgo.com")) {
                titleMatches.push({
                    url: actualUrl,
                    title
                });
            }
        }
        while((m = snippetRegex.exec(html)) !== null){
            snippets.push(m[1].replace(/<[^>]+>/g, "").trim());
        }
        for(let i = 0; i < titleMatches.length; i++){
            results.push({
                url: titleMatches[i].url,
                title: titleMatches[i].title,
                snippet: snippets[i] || ""
            });
        }
        // Fallback: DuckDuckGo Instant Answer
        if (results.length === 0) {
            const jr = await fetch(`https://api.duckduckgo.com/?q=${encodeURIComponent(query)}&format=json&no_html=1&skip_disambig=1`, {
                headers: {
                    "User-Agent": "Mozilla/5.0"
                },
                signal: AbortSignal.timeout(8000)
            });
            if (jr.ok) {
                const d = await jr.json();
                if (d.AbstractText) results.push({
                    url: d.AbstractURL || "",
                    title: d.Heading || query,
                    snippet: d.AbstractText
                });
            }
        }
        return results;
    } catch  {
        return [];
    }
}
// Direct scrape Ã¢â‚¬â€ no AI involved
async function doScrape(url) {
    try {
        new URL(url);
        const response = await fetch(url, {
            headers: {
                "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
                "Accept": "text/html,application/xhtml+xml,*/*;q=0.8"
            },
            signal: AbortSignal.timeout(10000)
        });
        if (!response.ok) return "";
        return extractTextFromHTML(await response.text()).slice(0, 5000);
    } catch  {
        return "";
    }
}
// Ask AI whether this task is a PURE text transformation with no external lookup needed.
// Defaults aggressively to web Ã¢â‚¬â€ only returns false for obvious transformations.
async function taskNeedsWebSearch(prompt, headers) {
    const { text } = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$ai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["generateText"])({
        model: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$ai$2d$sdk$2f$openai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__["openai"])("gpt-4o-mini"),
        prompt: `Is this spreadsheet column task a PURE TEXT TRANSFORMATION that can be done using ONLY the data already visible in the row Ã¢â‚¬â€ with no need to look up anything from the internet?

Pure transformations (reply "transform"): uppercase/lowercase, reformat dates, split/join text, translate language, math calculation, classify text that is fully visible in the row.

Web lookups (reply "web"): finding emails, phone numbers, website URLs, company info, people's names/titles, addresses, descriptions, prices, social media profiles Ã¢â‚¬â€ anything that needs NEW information not already in the row.

Column headers: ${headers.join(", ")}
Task: "${prompt}"

If there is ANY doubt, reply "web".
Reply with exactly one word: "web" or "transform".`
    });
    return !text.trim().toLowerCase().startsWith("transform");
}
/**
 * Validate that an extracted value is genuinely of the expected type.
 * Returns true if the value looks real, false if it looks fabricated or wrong format.
 */ function validateExtracted(value, prompt, context) {
    if (!value || value === "N/A" || value.trim().length < 2) return false;
    const lp = prompt.toLowerCase();
    const val = value.trim();
    // Format-specific validation
    if (lp.includes("email")) {
        if (!/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/.test(val)) return false;
        // Email domain must appear in context
        const domain = val.split("@")[1]?.split(".")[0];
        return !domain || context.toLowerCase().includes(domain.toLowerCase());
    }
    if (lp.includes("phone") || lp.includes("telephone") || lp.includes("mobile")) {
        if (!/[\d]{7,}/.test(val.replace(/[\s\-\(\)\+\.]/g, ""))) return false;
    }
    if (lp.includes("url") || lp.includes("website") || lp.includes("homepage")) {
        if (!/^https?:\/\//i.test(val) && !val.includes(".")) return false;
    }
    if (lp.includes("linkedin")) {
        if (!val.includes("linkedin.com")) return false;
    }
    // Ground truth: core of the value must appear somewhere in the scraped context
    const core = val.replace(/^https?:\/\/(www\.)?/, "").split(/[@/\s]/)[0].toLowerCase();
    if (core.length > 3 && !context.toLowerCase().includes(core)) return false;
    return true;
}
async function runAIColumn(colIdx, colType, prompt, cells, numRows, numCols, selectedRows, send) {
    if (!process.env.OPENAI_API_KEY) {
        send({
            type: "error",
            content: "OpenAI API key not configured."
        });
        return;
    }
    const colLabels = Array.from({
        length: numCols
    }, (_, i)=>getColLabel(i));
    const headerValues = Array.from({
        length: numCols
    }, (_, c)=>cells[`0-${c}`] || colLabels[c]);
    // Determine row scope: selected rows OR all data rows
    const rowsToProcess = selectedRows && selectedRows.length > 0 ? selectedRows.filter((r)=>r > 0 && r < numRows) : Array.from({
        length: numRows - 1
    }, (_, i)=>i + 1);
    if (rowsToProcess.length === 0) {
        send({
            type: "error",
            content: "No rows to process."
        });
        return;
    }
    const scopeLabel = selectedRows && selectedRows.length > 0 ? `${selectedRows.length} selected row${selectedRows.length > 1 ? "s" : ""}` : `all ${numRows - 1} rows`;
    // Decide pipeline: web search or pure derivation
    const isWebType = colType === "AI Web";
    const requiresWeb = isWebType || await taskNeedsWebSearch(prompt, headerValues);
    if (!requiresWeb) {
        // Ã¢â€â‚¬Ã¢â€â‚¬ Pure derivation (format, translate, categorize, calculate) Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬
        send({
            type: "thinking",
            content: `Ã°Å¸Â§Â  Deriving values for ${scopeLabel}...`
        });
        let rowContext = "";
        for (const r of rowsToProcess){
            const parts = [];
            for(let c = 0; c < numCols; c++){
                const v = cells[`${r}-${c}`];
                if (v) parts.push(`${colLabels[c]}: "${v}"`);
            }
            if (parts.length) rowContext += `Row ${r}: ${parts.join(" | ")}\n`;
        }
        const { text: fullText } = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$ai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["generateText"])({
            model: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$ai$2d$sdk$2f$openai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__["openai"])(process.env.OPENAI_MODEL || "gpt-4o"),
            system: "You are a spreadsheet formula engine. You ONLY transform or derive values from the data explicitly provided in the row. " + "You NEVER look up, invent, or guess external facts (emails, phone numbers, URLs, descriptions, addresses, etc.) that are not already present in the row data.",
            prompt: `Fill column ${getColLabel(colIdx)} for rows: ${rowsToProcess.join(", ")}.
Instruction: "${prompt}"
Headers: ${headerValues.join(", ")}

Row data:
${rowContext || "(none)"}

Return ONLY this JSON (no other text):
\`\`\`json
{"updates":[{"row":1,"value":"..."}],"summary":"..."}
\`\`\``
        });
        let parsed = null;
        try {
            const cb = fullText.match(/```json\s*([\s\S]*?)```/);
            if (cb) parsed = JSON.parse(cb[1].trim());
            if (!parsed) {
                const jm = fullText.match(/\{[\s\S]*\}/);
                if (jm) parsed = JSON.parse(jm[0]);
            }
        } catch  {}
        if (!parsed?.updates?.length) {
            send({
                type: "error",
                content: "Could not derive values. Try rephrasing your instruction."
            });
            return;
        }
        const updates = {};
        for (const { row, value } of parsed.updates)updates[`${row}-${colIdx}`] = value;
        send({
            type: "result",
            data: {
                success: true,
                updates,
                summary: parsed.summary || `Filled ${parsed.updates.length} cells`
            }
        });
        return;
    }
    // Ã¢â€â‚¬Ã¢â€â‚¬ Deterministic per-row web search + validation + retry pipeline Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬Ã¢â€â‚¬
    send({
        type: "thinking",
        content: `Ã°Å¸Å’Â Searching the web for ${scopeLabel}...`
    });
    const updates = {};
    let found = 0;
    for (const r of rowsToProcess){
        // Build entity string from existing row values
        const rowValues = [];
        for(let c = 0; c < numCols; c++){
            const v = cells[`${r}-${c}`];
            if (v && c !== colIdx) rowValues.push(v);
        }
        if (!rowValues.length) continue;
        const entityStr = rowValues.slice(0, 3).join(" ");
        send({
            type: "thinking",
            content: `Ã°Å¸â€Â Row ${r}: ${entityStr.slice(0, 50)}`
        });
        let finalValue = "N/A";
        // Convert the task description into focused search terms
        // e.g. "find emails of each school" Ã¢â€ â€™ "email contact"
        const searchTerms = prompt.replace(/\b(find|get|look up|search for|fetch|retrieve|what is|what are|of each row|of each|for each|for the|of the|please|show me|tell me|give me)\b/gi, "").replace(/\s+/g, " ").trim() || prompt;
        // Up to 2 search attempts with different queries
        const queries = [
            `${entityStr} ${searchTerms}`,
            `${rowValues[0]} ${searchTerms} official contact`
        ];
        console.log(`[RunColumn] Row ${r}: entity="${entityStr}" task="${prompt}" query="${queries[0]}"`);
        for(let attempt = 0; attempt < queries.length; attempt++){
            const query = queries[attempt];
            if (attempt > 0) {
                send({
                    type: "thinking",
                    content: `Ã°Å¸â€â€ž Row ${r}: retrying with refined query...`
                });
            }
            // Step 1 Ã¢â‚¬â€ always search (no AI choice)
            const results = await doSearch(query);
            let context = results.map((sr)=>`[${sr.title}] ${sr.snippet} (${sr.url})`).join("\n");
            console.log(`[RunColumn] Row ${r} attempt ${attempt}: got ${results.length} results, context length=${context.length}`);
            // Step 2 Ã¢â‚¬â€ always scrape the top result to get full page content
            // (snippets rarely contain structured data like emails/phones)
            if (results[0]?.url) {
                send({
                    type: "thinking",
                    content: `Ã°Å¸â€œâ€ž Reading: ${results[0].url.replace(/^https?:\/\//, "").slice(0, 55)}`
                });
                const page = await doScrape(results[0].url);
                if (page) {
                    context += "\n\n" + page;
                    console.log(`[RunColumn] Row ${r}: scraped ${page.length} chars from ${results[0].url}`);
                }
            }
            if (!context.trim()) continue;
            // Step 3 Ã¢â‚¬â€ strict AI extraction from real context only
            const { text } = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$ai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["generateText"])({
                model: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$ai$2d$sdk$2f$openai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__["openai"])("gpt-4o-mini"),
                system: "You are a data extraction tool. You output ONLY values that are explicitly and literally present in the provided web data. " + "You never infer, complete, guess, or generate any value that does not appear verbatim in the text. " + "If the requested value is absent, output exactly: N/A",
                prompt: `Web data (real, scraped):
---
${context.slice(0, 3500)}
---

Find the exact "${prompt}" for: ${rowValues.join(", ")}

Output rules:
- Output the raw value only (e.g. email address only, not "Email: user@example.com")
- The value MUST appear in the web data
- If not found: N/A`
            });
            const candidate = text.trim() || "N/A";
            // Step 4 Ã¢â‚¬â€ validate the extracted value is real and format-correct
            if (candidate !== "N/A" && validateExtracted(candidate, prompt, context)) {
                finalValue = candidate;
                break; // valid Ã¢â‚¬â€ stop retrying
            }
        // else: retry with next query
        }
        updates[`${r}-${colIdx}`] = finalValue;
        if (finalValue !== "N/A") found++;
    }
    send({
        type: "result",
        data: {
            success: true,
            updates,
            summary: `Found verified data for ${found} of ${rowsToProcess.length} rows`
        }
    });
}
async function runScrapeColumn(colIdx, sourceCol, cells, numRows, selectedRows, send) {
    const updates = {};
    let scraped = 0;
    const rowsToProcess = selectedRows && selectedRows.length > 0 ? selectedRows.filter((row)=>row > 0 && row < numRows) : Array.from({
        length: Math.max(0, numRows - 1)
    }, (_, i)=>i + 1);
    if (rowsToProcess.length === 0) {
        send({
            type: "error",
            content: "No rows to process."
        });
        return;
    }
    send({
        type: "thinking",
        content: `Scraping URLs from column ${getColLabel(sourceCol)} for ${rowsToProcess.length} row${rowsToProcess.length === 1 ? "" : "s"}...`
    });
    for (const r of rowsToProcess){
        const rawValue = cells[`${r}-${sourceCol}`] ?? "";
        const url = normalizeUrl(rawValue);
        if (!url) continue;
        send({
            type: "thinking",
            content: `Reading row ${r + 1}: ${url.replace(/^https?:\/\//, "").slice(0, 50)}`
        });
        try {
            const response = await fetch(url, {
                headers: {
                    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
                    "Accept": "text/html,application/xhtml+xml,*/*;q=0.8"
                },
                signal: AbortSignal.timeout(10000)
            });
            if (!response.ok) continue;
            const html = await response.text();
            const content = extractTextFromHTML(html).slice(0, 4000);
            if (content) {
                updates[`${r}-${colIdx}`] = content;
                scraped++;
            }
        } catch  {
        // skip failed URLs
        }
    }
    send({
        type: "result",
        data: {
            success: true,
            updates,
            summary: `Scraped ${scraped} URL${scraped !== 1 ? "s" : ""}`
        }
    });
}
function runRegexColumn(colIdx, sourceCol, pattern, cells, numRows, send) {
    const updates = {};
    let re;
    try {
        re = new RegExp(pattern);
    } catch  {
        send({
            type: "error",
            content: `Invalid regex pattern: ${pattern}`
        });
        return;
    }
    for(let r = 0; r < numRows; r++){
        const val = cells[`${r}-${sourceCol}`] ?? "";
        const m = val.match(re);
        updates[`${r}-${colIdx}`] = m ? m[1] ?? m[0] : "";
    }
    send({
        type: "result",
        data: {
            success: true,
            updates,
            summary: `Applied regex to ${numRows} rows`
        }
    });
}
function runNormalizeColumn(colIdx, colType, sourceCol, cells, numRows, send) {
    const updates = {};
    for(let r = 0; r < numRows; r++){
        const val = (cells[`${r}-${sourceCol}`] ?? "").trim();
        if (!val) continue;
        if (colType === "Normalize Domain") {
            try {
                updates[`${r}-${colIdx}`] = new URL(val.startsWith("http") ? val : `https://${val}`).hostname.replace(/^www\./, "");
            } catch  {
                updates[`${r}-${colIdx}`] = val;
            }
        } else {
            updates[`${r}-${colIdx}`] = val.toLowerCase().replace(/\b\w/g, (c)=>c.toUpperCase()).replace(/\bInc\b|\bLlc\b|\bLtd\b|\bCorp\b/g, (s)=>s.toUpperCase());
        }
    }
    send({
        type: "result",
        data: {
            success: true,
            updates,
            summary: `Normalized ${Object.keys(updates).length} values`
        }
    });
}
async function runReadFileColumn(colIdx, sourceCol, cells, numRows, send) {
    if (!process.env.OPENAI_API_KEY) {
        send({
            type: "error",
            content: "OpenAI API key not configured."
        });
        return;
    }
    const updates = {};
    let processed = 0;
    send({
        type: "thinking",
        content: `Ã°Å¸â€œâ€š Reading files from column ${getColLabel(sourceCol)}...`
    });
    for(let r = 0; r < numRows; r++){
        const fileRef = (cells[`${r}-${sourceCol}`] ?? "").trim();
        if (!fileRef) continue;
        send({
            type: "thinking",
            content: `Ã°Å¸â€œâ€ž Row ${r + 1}: ${fileRef.slice(0, 60)}`
        });
        try {
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            const content = [
                {
                    type: "text",
                    text: "Extract all text and meaningful content from this file. Return only the extracted content."
                }
            ];
            let skipAI = false;
            if (fileRef.startsWith("data:")) {
                // Local upload stored as data URL
                const match = fileRef.match(/^data:([^;]+);base64,(.+)$/);
                if (!match) continue;
                const mimeType = match[1];
                const base64Data = match[2];
                if (mimeType.startsWith("image/")) {
                    // Pass as data URL string Ã¢â‚¬â€ Buffer objects get stripped by AI SDK v6 Zod validation
                    content.push({
                        type: "image",
                        image: fileRef
                    });
                } else if (mimeType === "application/pdf") {
                    // AI SDK v6 uses "mediaType" (not "mimeType") for file parts
                    content.push({
                        type: "file",
                        data: fileRef,
                        mediaType: "application/pdf"
                    });
                } else {
                    // Plain text / CSV Ã¢â‚¬â€ decode directly, no AI needed
                    updates[`${r}-${colIdx}`] = Buffer.from(base64Data, "base64").toString("utf-8").slice(0, 4000);
                    processed++;
                    skipAI = true;
                }
            } else {
                // Remote URL
                const url = fileRef.startsWith("http") ? fileRef : `https://${fileRef}`;
                const ext = url.split("?")[0].split(".").pop()?.toLowerCase() ?? "";
                if ([
                    "jpg",
                    "jpeg",
                    "png",
                    "gif",
                    "webp"
                ].includes(ext)) {
                    // Pass URL as string Ã¢â‚¬â€ new URL() instances get stripped by AI SDK v6 Zod validation
                    content.push({
                        type: "image",
                        image: url
                    });
                } else if (ext === "pdf") {
                    const res = await fetch(url, {
                        headers: {
                            "User-Agent": "Mozilla/5.0"
                        },
                        signal: AbortSignal.timeout(15000)
                    });
                    if (!res.ok) continue;
                    const b64 = Buffer.from(await res.arrayBuffer()).toString("base64");
                    // AI SDK v6 uses "mediaType" (not "mimeType") for file parts; data must be a data URL string
                    content.push({
                        type: "file",
                        data: `data:application/pdf;base64,${b64}`,
                        mediaType: "application/pdf"
                    });
                } else {
                    // Plain fetch for text-based files
                    const res = await fetch(url, {
                        headers: {
                            "User-Agent": "Mozilla/5.0"
                        },
                        signal: AbortSignal.timeout(10000)
                    });
                    if (!res.ok) continue;
                    updates[`${r}-${colIdx}`] = extractTextFromHTML(await res.text()).slice(0, 4000);
                    processed++;
                    skipAI = true;
                }
            }
            if (!skipAI) {
                const { text } = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$ai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["generateText"])({
                    model: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$ai$2d$sdk$2f$openai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__["openai"])(process.env.OPENAI_MODEL || "gpt-4o"),
                    // eslint-disable-next-line @typescript-eslint/no-explicit-any
                    messages: [
                        {
                            role: "user",
                            content
                        }
                    ]
                });
                updates[`${r}-${colIdx}`] = text.trim();
                processed++;
            }
        } catch (err) {
            console.error(`[ReadFile] Row ${r} failed:`, err);
        }
    }
    send({
        type: "result",
        data: {
            success: true,
            updates,
            summary: `Processed ${processed} file${processed !== 1 ? "s" : ""}`
        }
    });
}
async function POST(request) {
    const body = await request.json();
    const { colIdx, colType, prompt = "", sourceCol = 0, regex = "", cells, numRows, numCols, selectedRows } = body;
    const requiresOpenAI = colType === "AI Agent" || colType === "AI Web" || colType === "Read File";
    if (requiresOpenAI && !process.env.OPENAI_API_KEY) {
        return new Response(`data: ${JSON.stringify({
            type: "error",
            content: "OpenAI API key not configured."
        })}\n\ndata: [DONE]\n\n`, {
            status: 503,
            headers: {
                "Content-Type": "text/event-stream",
                "Cache-Control": "no-cache",
                "Connection": "keep-alive"
            }
        });
    }
    const billableActionByColumnType = {
        "AI Agent": "run_column_ai",
        "AI Web": "run_column_ai",
        "Scrape Website": "run_column_scrape",
        "Read File": "run_column_read_file"
    };
    const actionKey = billableActionByColumnType[colType];
    let billingUserId = null;
    let billingTransactionId = null;
    const billingQuantity = getBillableRowCount(numRows, selectedRows);
    if (actionKey) {
        try {
            const billing = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$billing$2d$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["chargeCreditsForAction"])(request, actionKey, `Run column: ${colType} (${billingQuantity} row${billingQuantity === 1 ? "" : "s"})`, Math.max(1, billingQuantity));
            if (!billing.charge.skipped) {
                billingUserId = billing.user.id;
                billingTransactionId = String(billing.charge.transactionId);
            }
        } catch (error) {
            const message = error instanceof Error ? error.message : "Billing failed";
            return new Response(`data: ${JSON.stringify({
                type: "error",
                content: message
            })}\n\ndata: [DONE]\n\n`, {
                status: message.includes("Authentication required") ? 401 : message.includes("Not enough credits") ? 402 : 500,
                headers: {
                    "Content-Type": "text/event-stream",
                    "Cache-Control": "no-cache",
                    "Connection": "keep-alive"
                }
            });
        }
    }
    const encoder = new TextEncoder();
    const stream = new ReadableStream({
        async start (controller) {
            const send = (obj)=>controller.enqueue(encoder.encode(`data: ${JSON.stringify(obj)}\n\n`));
            try {
                if (colType === "AI Agent" || colType === "AI Web") {
                    await runAIColumn(colIdx, colType, prompt, cells, numRows, numCols, selectedRows, send);
                } else if (colType === "Scrape Website") {
                    await runScrapeColumn(colIdx, sourceCol, cells, numRows, selectedRows, send);
                } else if (colType === "Regex") {
                    runRegexColumn(colIdx, sourceCol, regex, cells, numRows, send);
                } else if (colType === "Normalize Company" || colType === "Normalize Domain") {
                    runNormalizeColumn(colIdx, colType, sourceCol, cells, numRows, send);
                } else if (colType === "Read File") {
                    await runReadFileColumn(colIdx, sourceCol, cells, numRows, send);
                } else {
                    send({
                        type: "error",
                        content: `Unknown column type: ${colType}`
                    });
                }
            } catch (error) {
                if (billingTransactionId && billingUserId) {
                    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$billing$2d$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["refundCredits"])(billingUserId, billingTransactionId, `Run column failed: ${colType}`);
                }
                console.error("[RunColumn] error:", error);
                send({
                    type: "error",
                    content: error instanceof Error ? error.message : "Failed to run column"
                });
            } finally{
                controller.enqueue(encoder.encode("data: [DONE]\n\n"));
                controller.close();
            }
        }
    });
    return new Response(stream, {
        headers: {
            "Content-Type": "text/event-stream",
            "Cache-Control": "no-cache",
            "Connection": "keep-alive"
        }
    });
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__0d6bdcb3._.js.map